export const mutations = `#graphql
    createPost:Boolean
    createReferralPost:Boolean
    createFindReferralPost:Boolean
    deletePost(id: ID!):Boolean
    bookmarkPost(id: ID!):Boolean
    applyPost(id: ID!):Boolean
    commentOnPost(id: ID!):Boolean
`;
